#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	int t,i,k;long int n;
	cin>>t;
	for(k=1;k<=t;++k)
	{
		cin>>n;
		if(floor(sqrt(n+1))=sqrt(n+1))
		cout<<
		while(floor(sqrt(n))!=sqrt(n))
		--n;
		cout<<"Case "<<k<<": "<<sqrt(n)-1<<endl;
	}
	return 0;
}
